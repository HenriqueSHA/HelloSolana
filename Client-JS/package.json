{
  "name": "hello-solana",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "ts-node src/client/main.ts",
    "build:program": "cargo-build-bpf --manifest-path=../Client-RS/src/program/Cargo.toml && mkdir -p dist/program && cp ../Client-RS/src/program/target/deploy/*.so dist/program/ || true",
    "clean:program": "cargo clean --manifest-path=../src/program/Cargo.toml && rm -rf dist",
    "test:program": "echo \"Use cargo tests direto ou adapte para cargo test-bpf se disponível\""
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4",
    "mz": "^2.7.0"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/mz": "^2.7.2",
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
